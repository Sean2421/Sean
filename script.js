// This file is intentionally almost empty — it's a starting point
// for you to add small interactive touches as you learn JavaScript.

// Example idea to try once you reach the JS section of MDN:
// Make the nav links smooth-scroll instead of jumping instantly.
//
// document.querySelectorAll('nav a').forEach(link => {
//   link.addEventListener('click', function (event) {
//     event.preventDefault();
//     const targetId = this.getAttribute('href');
//     document.querySelector(targetId).scrollIntoView({ behavior: 'smooth' });
//   });
// });

console.log("script.js is connected! Try editing this file and refreshing the page.");
